# dotfiles
dotfiles


# dotfiles内にうつすとき用のメモ
```bash
$ cd ~
$ cp ~/.vimrc ~/dotfiles/.vimrc
$ rm ~/.vimrc
$ ln -s ~/dotfiles/.vimrc ~/.vimrc
```

# instraller実行
```bash
$ chmod +x install.sh
$ ./install.sh
```
